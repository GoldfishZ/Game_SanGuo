stage: random
seed: 20260720
device: auto
num_workers: auto
rollout_steps: auto
minibatch_size: auto
max_updates: 5000
max_wallclock_minutes: 0  # 0 = 不自动停止；由 TensorBoard 观察后手动停止
eval_every: 20
eval_episodes: 64
eval_max_steps: 4096
eval_max_seconds: 300
checkpoint_every: 20
keep_last: 5
target_winrate: 0.85
patience: 10
min_delta: 0.01
learning_rate: 0.0003
learning_rate_final: 0.0001
entropy_coef: 0.05
entropy_coef_final: 0.01
epochs: 4
target_kl: 0.015
