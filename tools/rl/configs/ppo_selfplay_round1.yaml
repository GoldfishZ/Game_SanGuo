# 第一轮正式 self-play：先验证稳定增强，不在本轮惩罚平局。
stage: selfplay
seed: 20260721
device: auto
num_workers: auto
rollout_steps: auto
minibatch_size: auto
artifact_root: artifacts/rl/round_01

max_updates: 500
schedule_updates: 500
max_wallclock_minutes: 480

eval_every: 20
eval_episodes: 96
eval_max_steps: 4096
eval_max_seconds: 300
checkpoint_every: 10
keep_last: 8

learning_rate: 0.0003
learning_rate_final: 0.0001
entropy_coef: 0.05
entropy_coef_final: 0.01
epochs: 4
target_kl: 0.015

selfplay_current_ratio: 0.5
selfplay_pool_size: 24
selfplay_top_k: 8
selfplay_temperature: 0.25
selfplay_snapshot_every: 20

reward_draw: 0.0
